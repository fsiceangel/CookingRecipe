# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
DEPLOY_TO_GITHUB.md

# Private recipes — never committed to the repo or deployed to the live site.
# They are loaded only during local `npm run dev`.
public/data/recipes/private/

# Generated by predev/prebuild on every run; private recipe IDs can appear here
# during dev, so it must not be committed.
public/data/index.json
